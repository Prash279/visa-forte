// vitest.config.ts — test runner configuration for apps/web
// Runs in Node environment — no browser APIs needed for utility tests.

import { defineConfig } from 'vitest/config';
import path from 'path';

export default defineConfig({
  test: {
    environment: 'node',
    globals: true,
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
